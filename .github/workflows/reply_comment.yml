name: コメント返信

on:
  workflow_dispatch:
    inputs:
      comment_id:
        description: '返信先のコメントID'
        required: true
        type: string
      reply_text:
        description: '返信内容'
        required: true
        type: string

env:
  PYTHON_VERSION: '3.11'

jobs:
  reply-comment:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v4

      - name: Python環境をセットアップ
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Python依存関係をインストール
        run: |
          pip install --upgrade pip
          pip install google-auth google-api-python-client requests

      - name: コメントに返信
        env:
          YOUTUBE_CLIENT_ID: ${{ secrets.YOUTUBE_CLIENT_ID }}
          YOUTUBE_CLIENT_SECRET: ${{ secrets.YOUTUBE_CLIENT_SECRET }}
          YOUTUBE_REFRESH_TOKEN_23: ${{ secrets.YOUTUBE_REFRESH_TOKEN_23 }}
          REPLY_MODE: 'true'
          REPLY_COMMENT_ID: ${{ github.event.inputs.comment_id }}
          REPLY_TEXT: ${{ github.event.inputs.reply_text }}
        run: |
          python nenkin_comments.py

      - name: 成功時にSlack通知
        if: success()
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          if [ -n "$SLACK_WEBHOOK_URL" ]; then
            curl -X POST "$SLACK_WEBHOOK_URL" \
              -H "Content-Type: application/json" \
              -d "{
                \"text\": \"✅ *コメント返信が投稿されました*\n\n💬 コメントID: ${{ github.event.inputs.comment_id }}\n📝 返信: ${{ github.event.inputs.reply_text }}\"
              }"
          fi

      - name: エラー時にSlack通知
        if: failure()
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          if [ -n "$SLACK_WEBHOOK_URL" ]; then
            curl -X POST "$SLACK_WEBHOOK_URL" \
              -H "Content-Type: application/json" \
              -d "{
                \"text\": \"❌ *コメント返信に失敗しました*\n\n💬 コメントID: ${{ github.event.inputs.comment_id }}\n🔗 ワークフロー: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\"
              }"
          fi
