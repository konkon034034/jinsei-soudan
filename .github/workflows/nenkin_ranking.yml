name: 年金ランキング動画

on:
  schedule:
    # 毎日 JST 19:00 に実行 (UTC 10:00)
    - cron: '0 10 * * *'
  workflow_dispatch:
    inputs:
      test_mode:
        description: 'テストモード（TOP3のみ、YTスキップ）'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'
      skip_api:
        description: 'APIスキップ（ダミーデータでテスト）'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

env:
  PYTHON_VERSION: '3.11'

jobs:
  generate-ranking:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v4

      - name: Python環境をセットアップ
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: システム依存関係をインストール
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg fonts-noto-cjk

      - name: Python依存関係をインストール
        run: |
          pip install --upgrade pip
          pip install google-genai pydub pillow google-auth google-api-python-client requests gdown gtts anthropic

      - name: ランキング動画を生成
        env:
          TEST_MODE: ${{ github.event.inputs.test_mode || 'false' }}
          SKIP_API: ${{ github.event.inputs.skip_api || 'false' }}
          # Gemini API Key (有料枠)
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          # Gemini API Keys (1-42)
          GEMINI_API_KEY_1: ${{ secrets.GEMINI_API_KEY_1 }}
          GEMINI_API_KEY_2: ${{ secrets.GEMINI_API_KEY_2 }}
          GEMINI_API_KEY_3: ${{ secrets.GEMINI_API_KEY_3 }}
          GEMINI_API_KEY_4: ${{ secrets.GEMINI_API_KEY_4 }}
          GEMINI_API_KEY_5: ${{ secrets.GEMINI_API_KEY_5 }}
          GEMINI_API_KEY_6: ${{ secrets.GEMINI_API_KEY_6 }}
          GEMINI_API_KEY_7: ${{ secrets.GEMINI_API_KEY_7 }}
          GEMINI_API_KEY_8: ${{ secrets.GEMINI_API_KEY_8 }}
          GEMINI_API_KEY_9: ${{ secrets.GEMINI_API_KEY_9 }}
          GEMINI_API_KEY_10: ${{ secrets.GEMINI_API_KEY_10 }}
          GEMINI_API_KEY_11: ${{ secrets.GEMINI_API_KEY_11 }}
          GEMINI_API_KEY_12: ${{ secrets.GEMINI_API_KEY_12 }}
          GEMINI_API_KEY_13: ${{ secrets.GEMINI_API_KEY_13 }}
          GEMINI_API_KEY_14: ${{ secrets.GEMINI_API_KEY_14 }}
          GEMINI_API_KEY_15: ${{ secrets.GEMINI_API_KEY_15 }}
          GEMINI_API_KEY_16: ${{ secrets.GEMINI_API_KEY_16 }}
          GEMINI_API_KEY_17: ${{ secrets.GEMINI_API_KEY_17 }}
          GEMINI_API_KEY_18: ${{ secrets.GEMINI_API_KEY_18 }}
          GEMINI_API_KEY_19: ${{ secrets.GEMINI_API_KEY_19 }}
          GEMINI_API_KEY_20: ${{ secrets.GEMINI_API_KEY_20 }}
          GEMINI_API_KEY_21: ${{ secrets.GEMINI_API_KEY_21 }}
          GEMINI_API_KEY_22: ${{ secrets.GEMINI_API_KEY_22 }}
          GEMINI_API_KEY_23: ${{ secrets.GEMINI_API_KEY_23 }}
          GEMINI_API_KEY_24: ${{ secrets.GEMINI_API_KEY_24 }}
          GEMINI_API_KEY_25: ${{ secrets.GEMINI_API_KEY_25 }}
          GEMINI_API_KEY_26: ${{ secrets.GEMINI_API_KEY_26 }}
          GEMINI_API_KEY_27: ${{ secrets.GEMINI_API_KEY_27 }}
          GEMINI_API_KEY_28: ${{ secrets.GEMINI_API_KEY_28 }}
          GEMINI_API_KEY_29: ${{ secrets.GEMINI_API_KEY_29 }}
          GEMINI_API_KEY_30: ${{ secrets.GEMINI_API_KEY_30 }}
          GEMINI_API_KEY_31: ${{ secrets.GEMINI_API_KEY_31 }}
          GEMINI_API_KEY_32: ${{ secrets.GEMINI_API_KEY_32 }}
          GEMINI_API_KEY_33: ${{ secrets.GEMINI_API_KEY_33 }}
          GEMINI_API_KEY_34: ${{ secrets.GEMINI_API_KEY_34 }}
          GEMINI_API_KEY_35: ${{ secrets.GEMINI_API_KEY_35 }}
          GEMINI_API_KEY_36: ${{ secrets.GEMINI_API_KEY_36 }}
          GEMINI_API_KEY_37: ${{ secrets.GEMINI_API_KEY_37 }}
          GEMINI_API_KEY_38: ${{ secrets.GEMINI_API_KEY_38 }}
          GEMINI_API_KEY_39: ${{ secrets.GEMINI_API_KEY_39 }}
          GEMINI_API_KEY_40: ${{ secrets.GEMINI_API_KEY_40 }}
          GEMINI_API_KEY_41: ${{ secrets.GEMINI_API_KEY_41 }}
          GEMINI_API_KEY_42: ${{ secrets.GEMINI_API_KEY_42 }}
          # YouTube認証 (ftt357g チャンネル)
          YOUTUBE_CLIENT_ID: ${{ secrets.YOUTUBE_CLIENT_ID }}
          YOUTUBE_CLIENT_SECRET: ${{ secrets.YOUTUBE_CLIENT_SECRET }}
          YOUTUBE_REFRESH_TOKEN_23: ${{ secrets.YOUTUBE_REFRESH_TOKEN_23 }}
          # 通知
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          python nenkin_ranking.py

      - name: テスト用アーティファクトをアップロード
        if: ${{ github.event.inputs.test_mode == 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: ranking-video-test-${{ github.run_id }}
          path: |
            *.mp4
            *.mp3
            *.ass
          retention-days: 3

      - name: 成功時にDiscord通知
        if: success()
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          if [ -n "$DISCORD_WEBHOOK_URL" ]; then
            if [ "${{ github.event.inputs.test_mode }}" = "true" ]; then
              curl -X POST "$DISCORD_WEBHOOK_URL" \
                -H "Content-Type: application/json" \
                -d "{
                  \"content\": \"🧪 **ランキング動画テスト完了**\\n\\n📦 アーティファクト: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\"
                }"
            fi
          fi

      - name: 成功時にLINE通知
        if: success() && github.event.inputs.test_mode != 'true'
        env:
          LINE_CHANNEL_ACCESS_TOKEN: ${{ secrets.LINE_CHANNEL_ACCESS_TOKEN }}
        run: |
          if [ -n "$LINE_CHANNEL_ACCESS_TOKEN" ] && [ -f "video_url.txt" ]; then
            VIDEO_URL=$(cat video_url.txt)
            # file:// や YouTube URL以外はスキップ
            if [[ "$VIDEO_URL" == file://* ]] || [[ "$VIDEO_URL" != https://*youtube.com/* ]]; then
              echo "YouTube URLではないためLINE通知をスキップ: $VIDEO_URL"
              exit 0
            fi
            VIDEO_TITLE=""
            if [ -f "video_title.txt" ]; then
              VIDEO_TITLE=$(cat video_title.txt)
            fi
            # jqでJSONを正しく構築（単一行）
            MESSAGE="🏆 今日のランキング動画を投稿しました！ 📺 ${VIDEO_URL}"
            JSON_PAYLOAD=$(jq -n --arg msg "$MESSAGE" '{"messages": [{"type": "text", "text": $msg}]}')
            echo "送信するJSON: $JSON_PAYLOAD"
            RESPONSE=$(curl -s -w "\n%{http_code}" -X POST "https://api.line.me/v2/bot/message/broadcast" \
              -H "Authorization: Bearer $LINE_CHANNEL_ACCESS_TOKEN" \
              -H "Content-Type: application/json" \
              -d "$JSON_PAYLOAD")
            HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
            BODY=$(echo "$RESPONSE" | sed '$d')
            echo "HTTPステータス: $HTTP_CODE"
            echo "レスポンス: $BODY"
            if [ "$HTTP_CODE" = "200" ]; then
              echo "LINE通知を送信しました"
            else
              echo "LINE通知に失敗しました"
            fi
          else
            echo "LINE_CHANNEL_ACCESS_TOKENまたはvideo_url.txtがありません"
          fi

      - name: エラー時にDiscord通知
        if: failure()
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          if [ -n "$DISCORD_WEBHOOK_URL" ]; then
            curl -X POST "$DISCORD_WEBHOOK_URL" \
              -H "Content-Type: application/json" \
              -d "{
                \"content\": \"❌ **ランキング動画生成に失敗しました**\\n\\n🔗 ワークフロー: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\"
              }"
          fi
