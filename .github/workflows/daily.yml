name: Daily Video Generation

on:
  workflow_dispatch:
  schedule:
    - cron: '0 1 * * *'

jobs:
  generate-video:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg python3-dev
          pip install -r requirements.txt
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
          SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
          YOUTUBE_CHANNEL_ID: ${{ secrets.YOUTUBE_CHANNEL_ID }}
          BACKGROUND_IMAGE_ID: ${{ secrets.BACKGROUND_IMAGE_ID }}
          CHARACTER1_IMAGE_ID: ${{ secrets.CHARACTER1_IMAGE_ID }}
          CHARACTER2_IMAGE_ID: ${{ secrets.CHARACTER2_IMAGE_ID }}
          BGM_FILE_ID: ${{ secrets.BGM_FILE_ID }}
          EPISODE_NUMBER: 1
      
      - name: Debug: List files
        run: ls -F
      
      - name: Run video generation script
        run: python main.py
```

---

## 💾 ステップ4: 保存する

1. 画面を下にスクロール
2. 緑色の **「Commit changes...」** ボタンをクリック
3. ポップアップが出たら、もう一度 **「Commit changes」** ボタンをクリック

**画面イメージ:**
```
┌─────────────────────────────────────┐
│ Commit changes                      │
├─────────────────────────────────────┤
│ Create daily.yml                    │
│                                     │
│ [Commit directly to main branch]    │
│                                     │
│ [ Cancel ]  [✓ Commit changes]      │
└─────────────────────────────────────┘
```

---

## 📁 ステップ5: bakenami-video.ymlも作成

同じ手順を繰り返します：

1. リポジトリのトップページに戻る
2. **「Add file」** → **「Create new file」**
3. ファイル名に入力：
```
.github/workflows/bakenami-video.yml
