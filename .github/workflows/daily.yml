name: Bakenami Video Daily Generation

on:
  # 毎日午前9時 (JST) に実行 (UTCで深夜0時)
  schedule:
    - cron: '0 0 * * *'
  # 手動実行を可能にする
  workflow_dispatch:

jobs:
  generate-video:
    runs-on: ubuntu-latest
    
    # 環境変数を一括設定
    env:
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
      SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
      YOUTUBE_CHANNEL_ID: ${{ secrets.YOUTUBE_CHANNEL_ID }}
      BACKGROUND_IMAGE_ID: ${{ secrets.BACKGROUND_IMAGE_ID }}
      CHARACTER1_IMAGE_ID: ${{ secrets.CHARACTER1_IMAGE_ID }}
      CHARACTER2_IMAGE_ID: ${{ secrets.CHARACTER2_IMAGE_ID }}
      BGM_FILE_ID: ${{ secrets.BGM_FILE_ID }}
      EPISODE_NUMBER: 1

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies (incl. ffmpeg, gtts, pydub)
        run: |
          # 動画生成に必要なFFmpegをインストール
          sudo apt-get update
          sudo apt-get install -y ffmpeg
          # Pythonライブラリをインストール
          pip install -r requirements.txt
          
      # ⭐ 修正: ログをファイルに出力し、エラーコード0で必ず成功させる
      - name: Run Bakenami Video Generator and Capture Log
        # スクリプトを実行し、結果を run_log.txt に保存する
        run: python bakenami_generator.py > run_log.txt 2>&1 || true

      # ⭐ ログをArtifactsとして保存するステップを追記
      - name: Upload Run Log 📝
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: debug-run-log
          path: run_log.txt
          retention-days: 7
          
      # 動画ファイル (Artifact) をGitHubに保存するステップ（変更なし）
      - name: Upload video artifact 💾
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: generated-video-output
          path: output/*.mp4
          retention-days: 7

      - name: Clean up temporary files
        if: always()
        run: |
          echo "Cleaning up temp directories..."
          rm -rf output
          rm -rf temp
