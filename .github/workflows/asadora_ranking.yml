name: 朝ドラランキング動画自動生成 [スケジュール停止中]

on:
  # schedule:
  #   # 毎日朝9時（JST）に実行 = UTC 0:00
  #   - cron: '0 0 * * *'
  workflow_dispatch:  # 手動実行のみ
    inputs:
      mode:
        description: '処理モード'
        required: false
        default: 'auto'
        type: choice
        options:
          - auto
          - notebook
      test_mode:
        description: 'テストモード（TOP3で短い動画）'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'
      auto_upload:
        description: 'YouTubeに自動アップロード'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

env:
  PYTHON_VERSION: '3.11'
  TEST_MODE: ${{ github.event.inputs.test_mode || 'false' }}

jobs:
  generate-video:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v4

      - name: Python環境をセットアップ
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: システム依存関係をインストール
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            ffmpeg \
            fonts-noto-cjk \
            fonts-noto-cjk-extra \
            imagemagick \
            libmagickwand-dev

          # ImageMagickのポリシーを緩和
          sudo sed -i 's/rights="none" pattern="@\*"/rights="read|write" pattern="@*"/' /etc/ImageMagick-6/policy.xml || true
          sudo sed -i 's/<policy domain="path" rights="none" pattern="@\*"\/>/<policy domain="path" rights="read|write" pattern="@*"\/>/' /etc/ImageMagick-6/policy.xml || true

      - name: Python依存関係をインストール
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: 環境変数チェック
        env:
          YOUTUBE_REFRESH_TOKEN_27: ${{ secrets.YOUTUBE_REFRESH_TOKEN_27 }}
        run: |
          echo "=== 環境変数チェック ==="
          echo "YOUTUBE_REFRESH_TOKEN_27: $([ -n \"$YOUTUBE_REFRESH_TOKEN_27\" ] && echo '設定済み' || echo '未設定')"

      - name: 動画を生成・投稿
        env:
          # テストモード設定
          TEST_MODE: ${{ env.TEST_MODE }}
          GOOGLE_SERVICE_ACCOUNT_KEY: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_API_KEY_1: ${{ secrets.GEMINI_API_KEY_1 }}
          GEMINI_API_KEY_2: ${{ secrets.GEMINI_API_KEY_2 }}
          GEMINI_API_KEY_3: ${{ secrets.GEMINI_API_KEY_3 }}
          GEMINI_API_KEY_4: ${{ secrets.GEMINI_API_KEY_4 }}
          GEMINI_API_KEY_5: ${{ secrets.GEMINI_API_KEY_5 }}
          GEMINI_API_KEY_6: ${{ secrets.GEMINI_API_KEY_6 }}
          GEMINI_API_KEY_7: ${{ secrets.GEMINI_API_KEY_7 }}
          GEMINI_API_KEY_8: ${{ secrets.GEMINI_API_KEY_8 }}
          GEMINI_API_KEY_9: ${{ secrets.GEMINI_API_KEY_9 }}
          GEMINI_API_KEY_10: ${{ secrets.GEMINI_API_KEY_10 }}
          GEMINI_API_KEY_11: ${{ secrets.GEMINI_API_KEY_11 }}
          GEMINI_API_KEY_12: ${{ secrets.GEMINI_API_KEY_12 }}
          GEMINI_API_KEY_13: ${{ secrets.GEMINI_API_KEY_13 }}
          GEMINI_API_KEY_14: ${{ secrets.GEMINI_API_KEY_14 }}
          GEMINI_API_KEY_15: ${{ secrets.GEMINI_API_KEY_15 }}
          GEMINI_API_KEY_16: ${{ secrets.GEMINI_API_KEY_16 }}
          GEMINI_API_KEY_17: ${{ secrets.GEMINI_API_KEY_17 }}
          GEMINI_API_KEY_18: ${{ secrets.GEMINI_API_KEY_18 }}
          GEMINI_API_KEY_19: ${{ secrets.GEMINI_API_KEY_19 }}
          GEMINI_API_KEY_20: ${{ secrets.GEMINI_API_KEY_20 }}
          GEMINI_API_KEY_21: ${{ secrets.GEMINI_API_KEY_21 }}
          GEMINI_API_KEY_22: ${{ secrets.GEMINI_API_KEY_22 }}
          GEMINI_API_KEY_23: ${{ secrets.GEMINI_API_KEY_23 }}
          GEMINI_API_KEY_24: ${{ secrets.GEMINI_API_KEY_24 }}
          GEMINI_API_KEY_25: ${{ secrets.GEMINI_API_KEY_25 }}
          GEMINI_API_KEY_26: ${{ secrets.GEMINI_API_KEY_26 }}
          GEMINI_API_KEY_27: ${{ secrets.GEMINI_API_KEY_27 }}
          GEMINI_API_KEY_28: ${{ secrets.GEMINI_API_KEY_28 }}
          # Fish Audio TTS
          FISH_AUDIO_API_KEY: ${{ secrets.FISH_AUDIO_API_KEY }}
          UNSPLASH_ACCESS_KEY: ${{ secrets.UNSPLASH_ACCESS_KEY }}
          # BGM設定（Google DriveフォルダID、オプション）
          BGM_FOLDER_ID: ${{ secrets.BGM_FOLDER_ID }}
          YOUTUBE_CLIENT_ID: ${{ secrets.YOUTUBE_CLIENT_ID }}
          YOUTUBE_CLIENT_SECRET: ${{ secrets.YOUTUBE_CLIENT_SECRET }}
          # シニア口コミ専用 - TOKEN_27のみ
          YOUTUBE_REFRESH_TOKEN_27: ${{ secrets.YOUTUBE_REFRESH_TOKEN_27 }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          # schedule実行時は自動アップロード、手動実行時はauto_uploadフラグで制御
          SKIP_UPLOAD: ${{ github.event_name == 'schedule' && 'false' || (github.event.inputs.auto_upload != 'true' && 'true' || 'false') }}
        run: |
          python asadora_ranking.py

      - name: 成果物をアップロード
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: video-output-${{ github.run_id }}
          path: |
            *.mp4
            *.srt
          retention-days: 7
          if-no-files-found: ignore

      - name: Discord通知（成功）
        if: success()
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          AUTO_UPLOAD: ${{ github.event.inputs.auto_upload || 'false' }}
        run: |
          if [ -n "$DISCORD_WEBHOOK_URL" ]; then
            VIDEO_URL=""
            VIDEO_TITLE=""
            if [ -f "video_url.txt" ]; then
              VIDEO_URL=$(cat video_url.txt)
            fi
            if [ -f "video_title.txt" ]; then
              VIDEO_TITLE=$(cat video_title.txt)
            fi

            if [ "$AUTO_UPLOAD" = "true" ] && [[ "$VIDEO_URL" == https://*youtube.com/* ]]; then
              curl -X POST "$DISCORD_WEBHOOK_URL" \
                -H "Content-Type: application/json" \
                -d "{
                  \"content\": \"✅ **朝ドラランキング動画投稿完了！**\n━━━━━━━━━━━━━━━━━━\n\n📺 動画URL: ${VIDEO_URL}\n📝 タイトル: ${VIDEO_TITLE}\n\n━━━━━━━━━━━━━━━━━━\"
                }"
            else
              curl -X POST "$DISCORD_WEBHOOK_URL" \
                -H "Content-Type: application/json" \
                -d "{
                  \"content\": \"🎬 **朝ドラランキング動画生成完了（確認待ち）**\n━━━━━━━━━━━━━━━━━━\n\n📝 タイトル: ${VIDEO_TITLE}\n\n📥 **Artifactsから動画をダウンロード:**\n${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\n\n✅ 確認後、auto_upload=trueで再実行してください\n\n━━━━━━━━━━━━━━━━━━\"
                }"
            fi
          fi

      - name: Discord通知（失敗）
        if: failure()
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          if [ -n "$DISCORD_WEBHOOK_URL" ]; then
            curl -X POST "$DISCORD_WEBHOOK_URL" \
              -H "Content-Type: application/json" \
              -d "{\"content\": \"❌ **${{ github.workflow }}** 失敗\n━━━━━━━━━━━━━━━━━━\n\n🔗 ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\n\n━━━━━━━━━━━━━━━━━━\"}"
          fi
